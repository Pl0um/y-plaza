services:
    app:
        build:
            context: .
            dockerfile: dockerfile
        container_name: kreagency
        ports:
            - "3030:3030"
        networks:
            - vnet
        volumes:
            - ./:/usr/src/kreagency
            - /usr/src/app/node_modules
networks:
    vnet:
        driver: bridge